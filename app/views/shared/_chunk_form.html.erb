<%= form_for(@chunk) do |f| %>
<%= render 'shared/error_messages', object: f.object %>
<div class="field">
	<%= f.text_field :short_desc, placeholder: "New chunk to do..." %>
</div>
<%= f.submit "Create", class: "btn btn-large btn-primary" %>
<% end %>